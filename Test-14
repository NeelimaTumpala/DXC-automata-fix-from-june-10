1.The function deleteDuplicate(int *arr, int len) takes an array of length len (len > 0) as an input. It is supposed to remove duplicate integers from the input array arr such that for every distinct integer, the first occurrence is retained and all the duplicate elements following it are removed. For example, given the input array arr {2,3,2,2,5,6,6,7}, the expected output is {2,3,5,6,7}.

The function compiles successfully but fails to return the desired result because of logical errors.

Your task is to debug the program so that it passes all the test cases.

incorrect program:

#include<stdio.h>
int *deleteDuplicate(int* arr, int len){
	int count = 0, p, i, j, k;
	int originalLength = len;
	for(i=0;i<len;i++){
		for(j = i+1; j<len; j++){
			if(arr[j] == arr[i]){
				for(k=j; k<len-1; k++){
					arr[k] = arr[k+1];
				}
				len = len - 1;
				count = count + 1;
				j = i;
			}
		}
	}
	return (int *)arr;
}
int main()
{
	int arr[100],i,len,*a;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	a=deleteDuplicate(arr,len);
	for(i=0;i<len;i++)
	{
		printf("%d ",a[i]);
	}
}



2.The function printable(int num) is supposed to print the first ten multiples of the multiplication table of the input number num.

The function compiles fine but fails to return the desired result for some test cases.

Your task is to fix the program so that it passes all the test cases.

incorrect program:

#include<stdio.h>
void printTable(int num)
{
	int value = 0,i;
	for(i=0;i<10;i++)
	{
		value = num * i;
		printf("%d ", value);
	}
}
int main()
{
	int num;
	scanf("%d",&num);
	printTable(num);
}


fixed program:
#include<stdio.h>
void printTable(int num)
{
	int value = 0,i;
	for(i=1;i<=10;i++)
	{
		value = num * i;
		printf("%d ", value);
	}
}
int main()
{
	int num;
	scanf("%d",&num);
	printTable(num);
}



3.The function calculateGeneralLCM(int *arr, int len) accepts an integer array arr of lentgth len.

It is supposed to calculate and return the LCM of elements in the input array.

Another function calculateLCM(int a, int b) returns the LCM of two input numbers a and b.

Your task is to use the calculateLCM(int a, int b) function to complete the code in calculateGeneralLCM(int *arr, int len) so that it passes all test cases.

incorrect program:

#include<stdio.h>
int calculateLCM(int a, int b)
{
	int LCM = a;
	while(1)
	{
		if(LCM % b == 0 && LCM % a == 0)
			break;
		LCM++;
	}
	return LCM;
}

int calculateGeneralLCM(int *arr, int len)
{
	// write your code here and return LCM
	
}
int main()
{
	int res,arr[100],len,i;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	res=calculateGeneralLCM(arr,len);
	printf("%d",res);
}

fixed program:

#include<stdio.h>
int calculateLCM(int a, int b)
{
	int LCM = a;
	while(1)
	{
		if(LCM % b == 0 && LCM % a == 0)
			break;
		LCM++;
	}
	return LCM;
}

int calculateGeneralLCM(int *arr, int len)
{
	
	int lcm=arr[0],i;
	for(i=1;i<len;i++)
	{
	    lcm=calculateLCM(lcm,arr[i]);
	}
	return lcm;
}
int main()
{
	int res,arr[100],len,i;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	res=calculateGeneralLCM(arr,len);
	printf("%d",res);
}



4.The function findMinElement(int *arr1, int len1, int *arr2, int len2) accepts two integer arrays arr1, arr2 of length len1, len2, respectively.

It is supposed to return the smallest element in both the input arrays.

Another function sortArray(int *arr, int len) sorts the input array arr of length len in ascending order and returns it.

Your task is to use the sortArray(int *arr, int len) function to complete the code in findMinElement(int *arr1, int len1, int *arr2, int len2), so that it passes all the test cases.


incorrect program:

#include<stdio.h>
int* sortArray(int *arr, int len)
{
	int i=0,j=0,temp=0,index=0;
	for(i=0;i<len;i++)
	{
		for(j=i+1;j<len;j++)
		{
			if(arr[i] > arr[j])
			{
				temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
		}
	}
	return arr;
}

int findMinElement(int *arr1, int len1, int *arr2, int len2)
{
	// write your code here
}
int main()
{
	int res,arr1[100],len1,i,arr2[100],len2;
	scanf("%d%d",&len1,&len2);
	for(i=0;i<len1;i++)
	{
		scanf("%d",&arr1[i]);
	}
	for(i=0;i<len2;i++)
	{
		scanf("%d",&arr2[i]);
	}
	res=findMinElement(arr1,len1,arr2,len2);
	printf("%d",res);
}

fixed program:


#include<stdio.h>
int* sortArray(int *arr, int len)
{
	int i=0,j=0,temp=0;
	for(i=0;i<len;i++)
	{
		for(j=i+1;j<len;j++)
		{
			if(arr[i] > arr[j])
			{
				temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
		}
	}
	return arr;
}

int findMinElement(int *arr1, int len1, int *arr2, int len2)
{
	arr1=sortArray(arr1,len1);
	arr2=sortArray(arr2,len2);
	if(arr1[0]>arr2[0])
	{
	    return arr2[0];
	}
	return arr1[0];
}
int main()
{
	int res,arr1[100],len1,i,arr2[100],len2;
	scanf("%d%d",&len1,&len2);
	for(i=0;i<len1;i++)
	{
		scanf("%d",&arr1[i]);
	}
	for(i=0;i<len2;i++)
	{
		scanf("%d",&arr2[i]);
	}
	res=findMinElement(arr1,len1,arr2,len2);
	printf("%d",res);
}


5.The function checkGreatestFactor(int num) accepts an integer num as an input and is supposed to return the highest factor that is less than num.

It uses another function calculateFactor(int inputNumber) for calculating the factor of a number.

The function checkGreatestFactor looks fine but gives a compilation error.

Your task is to fix the program so that it passes all the test cases.



incorrect program:

#include<stdio.h>
int calculateFactor(int inputNumber);
int checkGreatestFactor(int num)
{
	if(num == 0)
		return 0;
	else
		return (calculateFactor(num));
}

int calculateFactor(inputNumber)
{
	int i = 0; maxFactor = 0;
	for(i=1;i<=inputnumber/2,i++)
	{
		if(inputnumber%i==0)
			maxFactor = i;
	}
	return maxFactor;
}
int main()
{
	int num;
	scanf("%d",&num);
	num=calculateFactor(num);
	printf("%d",num);
}


fixed program:

#include<stdio.h>
int calculateFactor(int inputNumber);
int checkGreatestFactor(int num)
{
	if(num == 0)
		return 0;
	else
		return (calculateFactor(num));
}

int calculateFactor(int inputNumber)
{
	int i = 0, maxFactor = 0;
	for(i=1;i<=inputNumber/2;i++)
	{
		if(inputNumber%i==0)
			maxFactor = i;
	}
	return maxFactor;
}
int main()
{
	int num;
	scanf("%d",&num);
	num=calculateFactor(num);
	printf("%d",num);
}


6.The function getArraySum(int *arr, int len) is supposed to calculate and return the sum of elements of the input array arr of length len (len > 0).

The functions compiles successfully but fails to return desired result because of logical errors.

Your task is to debug the program so that if passes all the test cases.

Assumption:

You may assume that sum of the elements of the array arr will not exceed the range of its data type.


incorrect program:

#include<stdio.h>
int getArraySum(int *arr, int len) 
{
	int sum = 0, i;
	for(i=0;i<len;i=i+1) {
		sum = arr[i];
	}
	return sum;
}

int main()
{
	int res,arr[100],len,i;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	res=getArraySum(arr,len);
	printf("%d",res);
}


fixed program:

#include<stdio.h>
int getArraySum(int *arr, int len) 
{
	int sum = 0, i;
	for(i=0;i<len;i=i+1) {
		sum += arr[i];
	}
	return sum;
}

int main()
{
	int res,arr[100],len,i;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	res=getArraySum(arr,len);
	printf("%d",res);
}


7.The function binarySearch(int* arr, int len, int target) performs the binary search algorithm to look for an element target in the input array arr of length len. If the element is found, the function returns the index of target in arr. If it is not found, the function returns -1.

The function seems to work well but goes into an infinite loop for some test cases.

Your task is to fix the program so that it passes all the test cases.

Note: if there is a Time Limit Exceeded error, it can be due to an infinite loop.



incorrect rpogram:

#include<stdio.h>
int binarySearch(int* arr, int len, int target)
{
	int lo = 0, hi = len - 1;
	while (lo <= hi)
	{
		int mid = lo + (hi-lo)/2;
		if(arr[mid] == target)
		{
			return mid;
		}
		else
			if(arr[mid] < target)
			{
				lo = mid;
			}
			else{
				hi = mid - 1;
			}
	}
	return -1;
}
int main()
{
	int res,arr[100],len,i,target;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	scanf("%d",&target);
	res=binarySearch(arr,len,target);
	printf("%d",res);
}

fixed progarm:

#include<stdio.h>
int binarySearch(int* arr, int len, int target)
{
	int lo = 0, hi = len - 1;
	while (lo <= hi)
	{
		int mid = lo + (hi-lo)/2;
		if(arr[mid] == target)
		{
			return mid;
		}
		else
			if(arr[mid] < target)
			{
				lo = mid+1;
			}
			else{
				hi = mid - 1;
			}
	}
	return -1;
}
int main()
{
	int res,arr[100],len,i,target;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	scanf("%d",&target);
	res=binarySearch(arr,len,target);
	printf("%d",res);
} 
