1.Find the syntax error in the below code without modifying the logic.

correct code:

#include <stdio.h>
int main() 
{
   int x = 1;
   switch (x)
   {
       case 1: printf("Choice is 1");
                 break;
       default: printf("Invalid choice");
                break; 
   }
   return 0;
}

2. Find the logical error in the below code.

we have to the below output:

1111
222
33
4

correct code:

#include<stdio.h>
int main ()
{
	int i, j, n = 5;
  	for(i=1; i<n; i++)
  	{
    	for(j=i;j<n;j++)
		{
			printf("%d", i);
   		}
    	printf("\n");
  	}
  	return 0;
}

3.Complete the below code by reusing the existing function.

Find the index of the equilibrium element in the given array. In an array equilibrium element is the one where the 
sum of all the elements to the left side is equal to the sum of all the elements in the right side.

Return Value:

1) Return -1 if no equilibrium element is found

2) In case there is more than one equilibrium element, return the element with the least index value.

You are required to complete the given code by reusing the existing function. You can click on Compile 
& run anytime to check the compilation/execution status of the program you can use printf to debug your code. 


correct code:

#include<stdio.h>
// Return the sum of elements from index 0 to (idx - 1)
int left_side_sum(int a[], int n, int idx)
{
    int sum = 0, i;
    for(i = 0; i < idx; i++)
    {
        sum += a[i];
    }

    return sum;
}

// Return the sum of elements from index (idx + 1) to (n - 1)
int right_side_sum(int a[], int n, int idx)
{
    int sum = 0, i;
    for(i = idx + 1; i < n; i++)
    {
        sum += a[i];
    }

    return sum;
}

// returns -1 if no equilibrium index found
int findEquilibriumIndex(int a[], int n)
{
    int i;
    int ls,rs;
    for(i=0;i<n;i++)
    {
        ls=+left_side_sum(a, n, i);
        rs=+right_side_sum(a, n, i);
        if(ls==rs)
        {
            return i;
            
        }
    }
    return -1;
}

int main() {
              //code
   int a[10], n, i;
   // get the elements count
   scanf("%d", &n);
   // get the array elements
   for(i=0; i<n; i++)
   {
       scanf("%d", &a[i]);
   }

   int equiIndex = findEquilibriumIndex(a, n);
   if(equiIndex != -1) {
       printf("%d", a[equiIndex]);
   }
   else
   {
   	printf("-1");
   }

   return 0;
}


4.Check for syntax error/ logical error and correct the error to get the desired output.

Given n, print from n to 0

correct code:

#include<stdio.h>
int main()
{
	int n;
  	scanf("%d", &n);
  	int i = n;
  	while(i>=0)
  	{
    	printf("%d\n", i);
    	i--;
  	}
 	return 0;
}

5.Find the factorial of a given number.

#include<stdio.h>
int main()
{
    long int fact = 1, n, i;
    scanf("%ld", &n);

    for(i =1; i <= n; i++)
    {
        fact = fact * i;
    }
    printf("%ld", fact);
    return 0;
}


6.Check whether the below program print the below pattern

1111

222

33

correct code:

#include<stdio.h>
int main()
{
    int i, j, n; 
    scanf("%d", &n);
    for(i = 1; i<=n; i++) 
    {   
        for(j = i; j<=n+1; j++)   
        {     
            printf("%d", i);  
        }  
        printf("\n");
    }
    return 0;
}
